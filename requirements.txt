# Zależności runtime — zsynchronizowane z pyproject.toml.
# Jedno źródło prawdy: pyproject.toml. Ten plik istnieje wyłącznie
# dla wygody `pip install -r requirements.txt` bez trybu editable.
sympy>=1.12
numpy>=1.24

# Opcjonalnie, tylko jeśli budujesz warstwę HTTP API (patrz pyproject.toml [project.optional-dependencies.api]):
# fastapi>=0.100
# uvicorn>=0.23
